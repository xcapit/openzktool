# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Build outputs
dist/
build/

# Circom/zkSNARK artifacts (large binary files)
*.wasm
*.zkey
*.r1cs
*.sym
*.ptau
*.wtns

# Generated artifacts directory (keep structure, ignore binaries)
circuits/artifacts/*.wasm
circuits/artifacts/*.zkey
circuits/artifacts/*.r1cs
circuits/artifacts/*.sym
circuits/artifacts/*.ptau
circuits/artifacts/*.wtns
circuits/artifacts/*_js/
circuits/artifacts/pot*

# Keep these important files (verification keys, proofs for demo)
!circuits/artifacts/*_vkey.json
!circuits/artifacts/proof.json
!circuits/artifacts/public.json
!circuits/artifacts/input.json

# Circuits build artifacts
circuits/simple_proof_js/
circuits/*.json
circuits/pot*.ptau
circuits/simple_proof_*.zkey

# EVM/Soroban compiled contracts (keep source, ignore build)
circuits/evm/*.json
!circuits/evm/Verifier.sol
soroban/target/
soroban/Cargo.lock

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log

# Test coverage
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp

# Secrets (NEVER COMMIT)
*.secret
*.key
*.pem
*.p12
*.pfx
private_keys/
.secrets/
credentials.json
*_credentials*.json
*_secret*.json
*.wallet
mnemonic.txt
seed.txt
private-key*.txt

# Web (Next.js)
web/.next/
web/out/
web/node_modules/
web/.vercel/

# Test artifacts
test_snapshots/
**/test_snapshots/

# OS
.DS_Store
Thumbs.db
.internal/
.aider*

# Rust build artifacts
contracts/target/
contracts/Cargo.lock

# Docker
docker-compose.override.yml
.docker/

# Stellar/Blockchain specific
.stellar/
.soroban/
stellar-accounts.json
*.identity

# Benchmarks results (can be large)
benchmarks/results/
!benchmarks/results/baseline/.gitkeep

# Security audit reports (private until disclosed)
security-reports/
audit-private/
